schema_version: 1
identity:
  organization: AnarchI
  component: Observation Contracts
  canonical_name: anarchi-observation-contracts
  responsibility: public evidence and cadence wire schemas
  authority: schema-only
repository:
  owner: AnarchI-Technologies
  name: anarchi-observation-contracts
  visibility: public
  status: canonical
dependencies: {none: true}
relationships:
  provides: [anarchi.evidence.v1, anarchi.evidence.v2, anarchi.incident-code.v1, anarchi.observer-cycle.v1]
  registered_in: [AnarchI-Technologies/anarchi-contract-registry]
boundaries:
  owns: [evidence envelope schema, cadence identity rules, correlation derivation rules]
  forbids: [runtime code, observation, routing, persistence, decisions, actions]
validation:
  deterministic: python -m json.tool schemas/evidence-envelope-v1.json && python -m json.tool schemas/evidence-envelope-v2.json
verification:
  required: {discovery: true, manifest: true, topology: true, plan: true, execution: true, stress: false, integration: true}
lineage:
  source: canonical-reconciliation-2026-08-11
